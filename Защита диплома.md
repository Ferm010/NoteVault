***слайд 1***

Здравствуйте уважаемая комиссия. Вашему вниманию представлена дипломная работа на тему "Разработка мобильного приложения для геймеров по и конструированию сборников модификаций" Выполнил студент группы ИС-46 Загвоцкий Данил Вячеславович. Дипломный руководитель Наумов Денис Анатольевич

***Слайд 2-3***
Прежде чем перейти к описанию программного продукта, позвольте кратно обозначить предметную область и цель дипломного проекта

Minecraft — это игра с открытой архитектурой, которая позволяет пользователям самостоятельно изменять её контент. 
Такие пользовательские дополнения называются модами.

Для совместной работы модов их объединяют в единые сборки — модпаки. 

Традиционно создание таких сборок требует технических навыков: ручной настройки модов, установка модлоадера, проверки версий игры, разрешения зависимостей между модами для работы.

Это создавало высокий порог входа для игроков, которые хотели экспериментировать с контентом, но не имели опыта или навыка как установить сборку

***слайд 4***
Для формирование точных требований к программному продукту и подтверждения актуальности поставленных задач, был проведен опрос целевой аудитории через платформу Google Forms

***Слайд 5***
Переходя к практической части реализации проекта необходимо рассмотреть архитектуру интерфейса самого продукта. Представленна диаграмма связей (MindMap), которая детализирует экранную структуру приложения и логикуку навигации между разделами

***Слайд 6***
Переходя к функциональному взаимодействию пользователя с приложением и охватывает весь спектр реализуемых функций. На ней отражены ключевые пользовательские сценарии: Регистрация и авторизация, поиск и фильтрация модов через Modrinth api, создание и редактирование модпаков с автоматическим разрешением зависимостей, экспорт в форматах .zip и .mrpack, а так же управление шаблонами, избранным и настройка профиля

***слайд 7***
Переходя  к этапу проектирования, необходимо обосновать выбор архитектурного паттерна для мобильного приложения в ходе анализа была рассмотрены 3 паттерна MVC, Clean Architecture и MVVM

- MVC обеспечивает быструю  разработку проосты экран, но приводит к перезагрузке активностей бизнес логикой, Это затрудняет модульное тестирование и усложняет масшатбирование при рост функционала
- Clean Architecture предлагает максимальную независимость слоев от фреймворков и удобство поддержки кодовой базы в  долгосрочной перспективе, однако ее многослойная структура требует значительного объема шаблоного кода
- MVVM Разделяет приложение на View, (отображение), ViewModel (управление состоянием логикой) и Model (данные сетевые запросы). Этот паттерн является официальным стандартом Google

Исходя из анализа архитектур был выбран MVVM по следующим причинам

- Совместимость с Jetpack Compose (фреймворк для создания пользовательского UI) идеально работает с реактивными потоками StateFlow. Это упрощает обновление интерфейса при посике модов, добавление компонентов в сборку или переключения тем оформленгия
- Тестируемость логика инкапсулирована в ViewModel, что позволяет разрабтывать unit-тесты

***Слайд 8-9***
Переходя к пользовательскому интерфейсу необходимо отметить, что этап создания интерфейса был реализован до начала написание кода, такой подход позволил зафиксировать логику навигации, проверить удобство взаимодействия с ключевыми экранами.
Для создания макета дизайна была выбрана Figma, что обеспечило единую дизайн систему на базе material desing 3. На слайде представлен пример дизайна. Полный утвержденный дизайн, доступен по QR-код

***слайд 10***
Переходя к уровню управление и хранение данными, необходимо обосновать выбор системы хранения информации для мобильного приложения в ходе анализа были рассмотрены

- Room обеспечивает высокую скорость работы в офлайн-режиме и строгую типизацию запросов, однако данные физически привязаны к одному устройству. Это исключает синхронизацию сборок между гаджетами и требует ручного управления миграциями схем при обновлении структуры модпаков.
- PostgreSQL предлагает мощные возможности. Тем не менее, её использование потребовало бы развёртывания собственного сервера, написания REST/gRPC API, настройки аутентификации и мониторинга отказоустойчивости
- В качестве основного хранилища был выбран cloud Firestore. Это решение дает Backend as a service. Обеспечивает встроенную real-time синхронизацию через метод "слушатели", автоматически масштабируется по нагрузку и нативную интеграцию с Firebase Аутинфекейшинен и Google Drive API

Также представлена ERD-диаграмма которая отражается структура данных. Хранение организование через collection: users, favorites, custom modpacks, templates

- Корневой уровень user/{userID} коллекция содержит базовый профиль аутеифицированного пользователя, которая хранит Email и Имя пользователя, createdAt- хранит время создания аккаунта, updateAt - хранит последние изменение аккаунта
- Подколлекция Favorites/{Project id} Хранит избранное пользователя. Вместо дублирования полного объекта мода из Modrinth хранится только необходимый минимум: `projectId`, `title`, `iconUrl`, `categories`, `versions`
- Подколлекция custom_modpack/{modpackId} Документ хранит метаданные сборки (`name`, `minecraftVersion`, `modLoader`) и массив `mods`. Обратите внимание: моды вложены прямо в документ как JSON-массив с полями `projectId`, `sha1`, `sha512`, `fileSize`. Такой подход выбран намеренно: он позволяет загружать полную структуру сборки за один запрос (`get`), что значительно ускоряет работу редактора и генератора `.mrpack`
- Подколлекция tempates/{templateId} Аналогична модпакам, но оптимизирована для быстрых стартов. Содержит минимальный набор полей (`name`, `mods` с `projectId` и `iconUrl`), что позволяет применять шаблон за миллисекунды без загрузки полных метаданных каждого мода.

**Слайд 11**
Переходя к этапу реализации проекта, необходимо обозначить технологический стек и инструменты, которые были выбраны для разработки мобильного приложения

- В ходе сравнительного анализа XML-разметки (Android Views), кроссплатформенного Flutter и нативного Jetpack Compose последнему было отдано предпочтение. Compose использует декларативный подход к описанию UI, что идеально сочетается с архитектурой MVVM и реактивными потоками `StateFlow`. Он обеспечивает глубокую интеграцию с Kotlin, нативную поддержку Material Design 3 из коробки и упрощает управление состоянием экранов редактора модпаков. Это сократило объём шаблонного кода и ускорило разработку отзывчивого интерфейса.
- В качестве основной IDE выбрана официальная среда от Google. Она предоставляет встроенный эмулятор Android-устройств, продвинутые инструменты, полную интеграцию с Kotlin-инструментами автодополнения и рефакторинга.
- Для хранения и совместной работы с исходным кодом выбрана платформа GitHub. Она обеспечивает наибольшее сообщество разработчиков, обширную экосистему и удобные инструменты code review. Репозиторий проекта доступен по QR-коду в Приложении В.5 пояснительной записки.
- Для координации задач, отслеживания и контроля календарного плана использовался YouGile. Платформа поддерживает Agile/Scrum методологии, предоставляет визуальные канбан-доски, диаграммы Ганта и встроенные отчёты по прогрессу.